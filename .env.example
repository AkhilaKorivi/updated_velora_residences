# Enquiry / backend configuration
# Leave the endpoint empty to use the mock/local submission flow.
VITE_ENQUIRY_ENDPOINT=

# Optional map provider configuration (kept configurable for later integration)
VITE_MAPS_API_KEY=
VITE_MAPS_EMBED_URL=